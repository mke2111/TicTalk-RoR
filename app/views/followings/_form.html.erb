<%= form_with(model: following) do |form| %>
  <% if following.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(following.errors.count, "error") %> prohibited this following from being saved:</h2>

      <ul>
        <% following.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :follower_id %>
    <%= form.number_field :follower_id %>
  </div>

  <div class="field">
    <%= form.label :followed_id %>
    <%= form.number_field :followed_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
