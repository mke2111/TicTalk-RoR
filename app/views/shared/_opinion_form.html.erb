<%= form_with(model: talk, local: true) do |form| %>
  <% if opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opinion.errors.count, "error") %> prohibited this opinion from being saved:</h2>

      <ul>
        <% opinion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content, "SAY SOMETHING", class: 'form-label' %>
    <%= form.text_area :content, class: 'text-area', placeholder: 'Compose a Talk' %>
  </div>

  <div class="actions">
    <%= form.submit 'Create Talk', class: 'submit-btn' %>
  </div>
<% end %>
